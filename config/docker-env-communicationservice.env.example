# Copy to docker-env-communicationservice.env (git-ignored) and fill in real values; never commit them.
LOGLEVEL_Q=INFO
MAILUSERNAME=your-ses-smtp-username
MAILPASSWORD=your-ses-smtp-password
MOTHERSLACKBOTTOKEN=your-slack-bot-token
