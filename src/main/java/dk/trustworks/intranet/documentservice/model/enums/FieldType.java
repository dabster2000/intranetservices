package dk.trustworks.intranet.documentservice.model.enums;

/**
 * Field types for template placeholders.
 */
public enum FieldType {
    STRING,
    TEXT,
    INTEGER,
    DECIMAL,
    CURRENCY,
    DATE,
    DATETIME,
    BOOLEAN,
    SELECT,
    USER_REFERENCE,
    EMAIL
}
