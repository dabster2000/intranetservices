package dk.trustworks.intranet.aggregates.invoice.selfbilled.model;

/** Who placed an assignment: a human, or the bulk same-company accept (AC2 allows bulk only same-company). */
public enum AssignmentSourceType { HUMAN, AUTO_SAMECOMPANY }
