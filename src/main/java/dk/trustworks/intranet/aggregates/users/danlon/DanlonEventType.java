package dk.trustworks.intranet.aggregates.users.danlon;

/** The four qualifying Danløn events (spec §4.1). */
public enum DanlonEventType {
    FIRST_EMPLOYMENT, RE_EMPLOYMENT, COMPANY_TRANSITION, SALARY_TYPE_CHANGE
}
